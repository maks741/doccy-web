import { FilterRecentDocumentsPipe } from './filter-recent-documents.pipe';

describe('FilterRecentDocumentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRecentDocumentsPipe();
    expect(pipe).toBeTruthy();
  });
});
